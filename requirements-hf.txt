# 給 Hugging Face Spaces 用的精簡版依賴。
# 本機請用 requirements.txt（外加安裝.bat 的第二段）。
#
# 採譜用 Basic Pitch（模型只有 228 KB、附在套件裡、CPU 上一首歌約 6 秒）。

# basic-pitch 走 ONNX 需要的東西（這些本身不會拉 TensorFlow）
onnxruntime
importlib_resources
resampy
# pretty_midi 內部會 import six，但它是用 --no-deps 裝的，six 不會跟著進來，
# 少了它 basic_pitch 一 import 就掛。scikit-learn / decorator / typing_extensions
# 由 librosa 帶進來，不用另外寫。
six

# 音訊與 MIDI
librosa
soundfile>=0.12
mido
numpy

# YouTube 連結（雲端的 ffmpeg 由 Dockerfile 用 apt 裝）
yt-dlp
