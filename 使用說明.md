# 三軌樂譜產生器 — 一首歌 →《倩女幽魂》編曲三軌 MML

丟一首歌（MP3、YouTube 連結、MIDI，或別人寫好的 MML），自動改編成三條單音旋律線，
分別貼進遊戲編曲介面的 **音軌A / 音軌B / 音軌C**。每軌上限 3000 字。

三軌是**一起組成一首曲子**，同時播放，不是「主旋律加伴奏」。

---

## 一、每次使用

1. 點兩下 `啟動網頁.bat`，瀏覽器會打開 <http://127.0.0.1:8765>。
2. 上面先選**樂器**：鋼琴、瑤箏、箜篌、電吉他、貝斯、笛子、小提琴、架子鼓。
3. 把 MP3 拖進去，或貼 YouTube 連結，或貼現成的 MML 文字。
4. 完成後可以試聽、調整。
5. **三軌各自按「複製」，分別貼進遊戲的 音軌A / 音軌B / 音軌C。**
速度（`t`）已經寫進三軌開頭，遊戲會照著跑，**不用去動「音速」旋鈕**。

> 給什麼音檔最像？該樂器的獨奏 Cover > 鋼琴 Cover > 原曲。
> 原曲有人聲和鼓，機器會把它們也聽成音符，結果會亂。

---

## 二、遊戲的規矩（會影響輸出）

- 樂譜欄**不收**中文、中文標點，以及 `￥ $ @ * \ [ ] | %` 這些字元。
  所以**不要貼整段 `MML@…,…;`**（開頭的 `@` 和逗號都是不合規字元），要貼單軌。
- 每軌 3000 字，三軌分開算。
- 鍵盤範圍是 C1～C8，程式不會寫超出這個範圍的音。
- 速度寫成 `t` 放在每一軌開頭（三軌都要有，只寫一軌其他兩軌會跑掉）。
- **沒有 `l`（預設音長）指令**，所以每個音、每個休止符都必須自己標長度。
  寫了 `l16` 又用裸音符的話，遊戲會把裸音符當成四分音符：音高全對、節奏全平，
  整首變成一樣長的「咚咚咚」，各軌長度不一致還會互相對不上。

---

## 三、結果不理想時（畫面上的「調整」）

| 症狀 | 怎麼調 | 指令參數 |
|---|---|---|
| 想更豐富 / 更簡單 | **改「每軌字數上限」**：程式會自動挑塞得進去的最豐富版本 | `--limit 4000` |
| 整首偏快或偏慢 | 速度改「手動」，填那首歌的 BPM | `--bpm 92` |
| 整體高或低了一個八度 | 八度：升／降一個八度 | `--octave-shift -1` |
| 太吵、雜音多 | 「B／C 軌密度」往疏拉、「雜音門檻」往右拉 | `--harmony-min 1/8 --min-vel 35` |
| 太小聲 / 太大聲 | 「整體音量」滑桿 | `--volume -3` |
| 副歌高音被伴奏蓋掉 | 「副歌讓路」選「強」 | `--climax 2` |
| 想要伴奏一直陪著 | 「副歌讓路」選「關閉」 | `--climax 0` |
| 三連音為主的歌 | 節奏格線：三連音 | `--grid 3` |

改完按「重新產生」，不用重新採譜，幾秒就好。

---

## 四、它做了什麼

音檔 → AI 聽出每個音（預設 Spotify Basic Pitch）→ 丟掉太小聲、太短的雜音 →
抓速度、把音貼到節拍格線 → 分成三條線（每一刻最高的給 A 軌、最低的給 C 軌、中間留一個給 B 軌）→
用整首的力度分佈配音量（以樂句為單位變化）→ 寫成 MML。

字數上限是「豐富度預算」：從最豐富的版本開始往下試，挑第一個塞得進上限的，
而且**每軌各自挑**，不會因為一軌超標就把三軌都簡化。

---

## 五、限制

- 三條線都是**單音**。同一時刻超過三個音，多的一定會被丟掉，這是三軌的硬限制。
- 網頁試聽只是簡單合成，跟遊戲音色不一樣，**以遊戲裡聽到的為準**。
- 採譜是機器聽的，人聲、鼓、破音多的原曲會亂。
- 架子鼓是用起音偵測分頻帶抓的（不是 AI 模型），複雜的鼓組會誤判。
- 網頁只在你自己的電腦上跑（127.0.0.1），別人連不到。
- 網頁試聽用的是很薄的三角波，聽不太出不協和音；遊戲的音色有延音、泛音多，
  差半音的音會很刺耳。程式已經會自動把三軌之間差半音的重疊音修掉。

---

## 六、第一次安裝

點兩下 **`安裝.bat`**。

不要只跑 `pip install -r requirements.txt`：basic-pitch 必須用 `--no-deps` 裝，
直接裝會把 TensorFlow 一起拉進來、並把 numpy 降到 1.x，環境裡其他套件會壞掉。
`安裝.bat` 已經處理好順序。

`.bat` 都已指定用 `ai24` 環境的 python。自己打指令的話要先 `conda activate ai24`。

---

## 七、用到的資源

| 東西 | 用途 | 授權 |
|---|---|---|
| [Spotify Basic Pitch](https://github.com/spotify/basic-pitch) | 預設採譜模型（跑內附的 ONNX 版） | Apache-2.0 |
| [yt-dlp](https://github.com/yt-dlp/yt-dlp) + [FFmpeg](https://ffmpeg.org/) | YouTube 連結抓音訊 | Unlicense / LGPL |
| [librosa](https://librosa.org/)、[mido](https://mido.readthedocs.io/)、[soundfile](https://python-soundfile.readthedocs.io/) | 音訊與 MIDI | ISC / MIT / BSD |
| [NumPy](https://numpy.org/)、[ONNX Runtime](https://onnxruntime.ai/) | 數值運算、模型推論 | BSD / MIT |

網頁本身只用 Python 內建的 `http.server`，沒有其他框架；試聽是瀏覽器的 Web Audio API 即時合成。

檔案：`mabi3.py`（轉換核心）、`server.py`（網頁後端）、`index.html`（介面）、
`安裝.bat`、`啟動網頁.bat`、`轉換.bat`（拖檔案上去直接轉）。
